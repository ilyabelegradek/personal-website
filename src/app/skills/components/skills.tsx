import { Typography } from "@mui/material";

export default function Skills() {
  return (
    <main className="flex flex-col">
      <Typography>React</Typography>
      <Typography variant="advantage">Kotlin</Typography>
      <Typography>Java</Typography>
    </main>
  );
}
